name: Deploy Release

on:
  workflow_call:
    inputs:
      version-name:
        required: true
        type: string
      tag-name:
        required: true
        type: string
      release-notes:
        required: false
        type: string
        default: 'TBD'
      publish-prerelease:
        required: false
        default: false
        type: boolean

jobs:
  build-usd2508:
    name: Build for USD v25.08
    uses: ./.github/workflows/build-usd.yml
    with:
      usd-version: 25.08
      upload-install-artifacts: true
      run-graphical-tests: true

  build-usd2505:
    name: Build for USD v25.05
    uses: ./.github/workflows/build-usd.yml
    with:
      usd-version: 25.05
      upload-install-artifacts: true
      run-graphical-tests: true

  build-usd2502:
    name: Build for USD v25.02
    uses: ./.github/workflows/build-usd.yml
    with:
      usd-version: 25.02
      upload-install-artifacts: true
      run-graphical-tests: true

  build-usd2411:
    name: Build for USD v24.11
    uses: ./.github/workflows/build-usd.yml
    with:
      usd-version: 24.11
      upload-install-artifacts: true
      run-graphical-tests: true

  build-houdini20:
    name: Build for Houdini 20.0
    uses: ./.github/workflows/build-houdini20.yml

  build-blender50:
    name: Build for Blender 5.0
    uses: ./.github/workflows/build-blender50.yml

  deploy-release:
    name: Deploy Release
    needs: [build-usd2508, build-usd2505, build-usd2502, build-usd2411, build-houdini20, build-blender50]
    runs-on: ubuntu-latest

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Deploy release
        uses: softprops/action-gh-release@d4e8205d7e959a9107da6396278b2f1f07af0f9b
        with:
          name: ${{ inputs.version-name }}
          body: ${{ inputs.release-notes }}
          files: |
            *.tar.gz
            *.zip
          fail_on_unmatched_files: true
          tag_name: ${{ inputs.tag-name }}
          prerelease: ${{ inputs.publish-prerelease }}
          draft: ${{ ! inputs.publish-prerelease }}
          make_latest: true
